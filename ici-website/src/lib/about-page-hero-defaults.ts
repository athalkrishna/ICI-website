/** Fixed /about hero copy — eyebrow + heading only (body lives in Our story section). */
export const ABOUT_HERO_DEFAULTS = {
  hero_eyebrow: 'About the Institute',
  hero_heading: 'Coaching education with a soul and a standard',
} as const;

export const ABOUT_HERO_FIELD_KEYS = Object.keys(ABOUT_HERO_DEFAULTS);
